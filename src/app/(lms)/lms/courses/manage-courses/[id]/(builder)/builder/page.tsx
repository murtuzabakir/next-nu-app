"use client";
import React from "react";

const page = () => {
  return (
    <div>
      existing course builder
      <button onClick={() => {}}> here too</button>
    </div>
  );
};

export default page;
